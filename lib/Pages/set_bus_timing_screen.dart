import 'package:flutter/material.dart';

class SetBusTimingScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Set Bus Timing Screen'));
  }
}
