import 'package:flutter/material.dart';

class MyBusPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('My Bus')),
      body: Center(child: Text('My Bus Details')),
    );
  }
}
