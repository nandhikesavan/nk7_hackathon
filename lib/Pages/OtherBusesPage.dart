import 'package:flutter/material.dart';

class OtherBusesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Other Buses')),
      body: Center(child: Text('Other Buses Details')),
    );
  }
}
